{
  "name": "itdone-template-static",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "serve -n -l tcp://0.0.0.0:${PORT:-3000} .",
    "dev": "serve -n -l tcp://0.0.0.0:${PORT:-3000} .",
    "build": "node check.js"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
